/// <reference types="node" />
import { EventEmitter } from 'events';
import { Session } from '../index';
export declare const CONNECT: string;
export declare const DISCONNECT: string;
export declare abstract class BaseStore extends EventEmitter {
    protected prefix: string;
    constructor(prefix?: string);
    abstract get(sid: string): Promise<Session>;
    set(sid: string, sess: Session, ttl?: number): Promise<any>;
    abstract destroy(sid: string): Promise<void>;
}
